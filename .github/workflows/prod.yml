name: Validate and deploy production

on:
  pull_request:
    branches:
      - main
    paths:
      - "api/**"
      - "web/**"
      - "packages/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/prod.yml"
  push:
    branches:
      - main
    paths:
      - "api/**"
      - "web/**"
      - "packages/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/prod.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: prepvilla-app-production
  cancel-in-progress: false

env:
  AWS_REGION: "eu-west-1"
  AWS_WORKLOAD_PROFILE: "prod-prepvilla"
  WORKSPACE: "prod"
  IMAGE_TAG: ${{ github.sha }}
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "24"
  DJANGO_SETTINGS_MODULE: "config.settings.test"
  DJANGO_ENV: "test"
  DJANGO_DEBUG: "1"
  DJANGO_SECRET_KEY: "ci-secret-key"
  FRONTEND_DOMAIN: "prepvilla.info"
  API_DOMAIN: "api.prepvilla.info"
  FRONTEND_BUCKET: "prepvilla-prod-frontend"
  NEXT_PUBLIC_API_BASE_URL: "https://api.prepvilla.info"
  NEXT_PUBLIC_WS_BASE_URL: "wss://api.prepvilla.info"

jobs:
  changes:
    name: Detect changed applications
    runs-on: ubuntu-latest
    timeout-minutes: 3
    outputs:
      web: ${{ github.event_name == 'workflow_dispatch' || steps.filter.outputs.web == 'true' }}
      api: ${{ github.event_name == 'workflow_dispatch' || steps.filter.outputs.api == 'true' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect changes
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            web:
              - "web/**"
              - "packages/**"
              - "package.json"
              - "package-lock.json"
              - ".github/workflows/prod.yml"
            api:
              - "api/**"
              - "scripts/deploy/api.sh"
              - "scripts/migrate.sh"
              - ".github/workflows/prod.yml"

  web:
    name: Validate and build web
    needs: changes
    if: needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint web
        run: npm run lint --workspace web

      - name: Typecheck web
        run: npm run typecheck --workspace web

      - name: Test web
        run: npm run test --if-present --workspace web

      - name: Build static web application
        run: npm run build:static --workspace web

      - name: Upload web build
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: web-build-${{ github.sha }}
          path: web/out
          if-no-files-found: error
          retention-days: 1

  api:
    name: Validate and test API
    needs: changes
    if: needs.changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: api/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install --disable-pip-version-check -r api/requirements.txt

      - name: Check Django configuration and migrations
        working-directory: api
        run: |
          python manage.py makemigrations --check --dry-run
          python manage.py check

      - name: Run backend tests
        working-directory: api
        run: python manage.py test

  deploy-web:
    name: Deploy web to production
    needs:
      - changes
      - web
    if: github.event_name != 'pull_request' && needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download web build
        uses: actions/download-artifact@v8
        with:
          name: web-build-${{ github.sha }}
          path: web/out

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          role-session-name: prepvilla-app-prod-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Publish web assets
        run: |
          aws s3 sync web/out/ "s3://${FRONTEND_BUCKET}/" \
            --region "${AWS_REGION}" \
            --delete \
            --exclude "*.html" \
            --exclude "_next/static/*"
          aws s3 sync web/out/ "s3://${FRONTEND_BUCKET}/" \
            --region "${AWS_REGION}" \
            --delete \
            --exclude "*" \
            --include "*.html" \
            --cache-control "public,max-age=0,s-maxage=60,must-revalidate"
          if [ -d web/out/_next/static ]; then
            aws s3 sync web/out/_next/static/ "s3://${FRONTEND_BUCKET}/_next/static/" \
              --region "${AWS_REGION}" \
              --delete \
              --cache-control "public,max-age=31536000,immutable"
          fi

      - name: Invalidate CloudFront cache
        run: |
          distribution_id="$(aws cloudfront list-distributions \
            --query "DistributionList.Items[?Aliases.Items!=null && contains(Aliases.Items, '${FRONTEND_DOMAIN}')].Id | [0]" \
            --output text)"
          test -n "${distribution_id}"
          test "${distribution_id}" != "None"
          aws cloudfront create-invalidation \
            --distribution-id "${distribution_id}" \
            --paths "/*"

  deploy-api:
    name: Deploy API to production
    needs:
      - changes
      - api
    if: github.event_name != 'pull_request' && needs.changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: production
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          role-session-name: prepvilla-api-prod-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}
          aws-profile: ${{ env.AWS_WORKLOAD_PROFILE }}

      - name: Build and deploy API
        env:
          BACKEND_DIR: api
          REGION: ${{ env.AWS_REGION }}
        run: bash scripts/deploy/api.sh "${WORKSPACE}"
